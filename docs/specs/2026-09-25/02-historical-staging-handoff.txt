I checked the current repository and Railway state. The source implementation is far ahead of what staging is actually running, so what remains is mainly certification, deployment, runtime proof, and a few operational hardening items—not another major rebuild.
1. Certify the exact latest integration head c61ef387ebce…. This is the biggest immediate task. The integration branch is currently 42 commits ahead and 0 behind the staging branch. The previous cert build at 9c0a09b… successfully ran 270 focused tests + 12/12 readiness checks, but its disposable HTTP healthcheck failed afterward. Since c61ef… contains later ML-starvation/recovery and provenance work, that exact SHA still needs a clean certification run. The repository itself records a broader local result of 1,365 passed, 1 optional skip, 0 failed, 0 errors.
2. Advance the watched staging branch from 2938f7fa… to c61ef387…. Do this only after the exact-head certification is green. The four actual staging services are currently healthy, but all four are still running 2938f7fa…, so none of the last 42 commits are live in staging yet.
3. Move staging PostgreSQL to the current Alembic head 0041_broker_connection_registry. This must include the new 0040 cross-channel paper-receipt provenance migration plus 0041. Then re-run the required schema admission check and verify there is exactly one Alembic head. The release documentation explicitly says fresh runtime proof for 0041 is still required.
4. Pre-stage the release-source SHA on every staging role before deployment. Update EXPECTED_RELEASE_COMMIT/branch expectations on web/front-door, worker, engine, and analytics first, then fast-forward the branch. This prevents another release_source_gate BLOCKED mismatch like the one you encountered earlier.
5. Boot all four staging roles on exactly the same SHA. Confirm web/front-door, worker, engine, and analytics all report c61ef387…, source-gate PASS, schema-gate PASS, 0041 head, no startup exceptions, and no hidden old container still serving traffic.
6. Re-test the ML behavior live on the new build. Current staging is still on the older code and its engine logs show the old diagnostic behavior—including effective_thresholds ... ml:0.850 while the model’s certified raw threshold is about 0.6293. The new branch contains the starvation detector, challenger telemetry, bounded recovery, and durable recovery provenance. We need to prove in staging that these work as intended and that recovery signals remain paper-only at every broker path.
7. Verify that ML recovery cannot leak into real execution. Test Telegram, website manual MT5 execution, auto execution, copy execution, broker routing, and every execution entrypoint. Any signal marked as starvation/recovery must have its broker CTA removed and must fail closed if someone tries to execute it programmatically.
8. Verify multi-asset market-data behavior during actual open sessions. Crypto and FX are already producing usable candles. Stocks, indices, and commodities need another runtime pass while their markets are open. Verify source timestamps, fresh quote failover, FMP/FCS/TwelveData/Yahoo/provider fallbacks, market-hours behavior, no stale delivery, and no asset class silently collapsing back to crypto-only behavior.
9. Run the complete web-delivery E2E. Generate a genuinely fresh eligible signal → final quote validation → canonical profile/tier checks → web receipt → web signal feed → lifecycle tracking → TP/SL/outcome event → notification center. Confirm no Telegram identity is required for a web-only account.
10. Run the complete cross-channel paper-trading E2E. A web-only user should receive a web signal receipt, auto-open a paper position when eligible, create paper_trade_attempts with delivery_id=NULL plus web receipt provenance, receive web paper notifications, see history/performance, retry an eligible signal, close all, update settings, and reset—all without fabricating a Telegram delivery.
11. Verify Telegram + web deduplication. A linked user who receives the same signal through both channels must still have one logical signal, one lifecycle, one canonical account, one paper position maximum, shared daily quotas, and no duplicate outcome/performance counting.
12. Exercise the new web broker workflow E2E. Link MT5 from the website, verify encrypted credential persistence, MetaApi account creation/recovery, broker status, explicit execution-risk consent, fixed lot/provider settings, Premium manual execution, VIP auto/copy restrictions, canonical execution quota, canonical evidence, broker ledger, and paper mirror. The manual website Trade button must continue to go through the same central ExecutionGate rather than directly calling the broker.
13. Verify web tier parity end-to-end. Free must not receive exact entry, SL, full confidence, restricted lifecycle analytics, or higher TP levels from the API—not merely have them hidden in the browser. Premium/VIP/Professional visibility, asset classes, delivery delays, paper features, and broker features need spot checks against Telegram.
14. Smoke-test the remaining cross-channel product parity that was just added. That includes subscription auto-renew cancellation, successful-only referral rewards, travel mode, VIP research/elite controls, billing, Paystack, account/security settings, quality/shadow analytics, broker registry, signal feedback, professional API signal feed, and account-only users that have never linked Telegram.
15. Run real staging Paystack tests. Test one app/web-created canonical user checkout and one Telegram-linked checkout. Confirm server-side price resolution, signed webhook activation, durable receipt creation, receipt email, cancellation/auto-renew behavior, referral conversion only after successful payment, and no duplicate subscription/account.
16. Run real email/auth smoke tests. Email verification, magic login, reset, transactional receipts, MFA/recovery codes, device revocation, and retry/dead-letter behavior still need live-provider evidence rather than unit tests alone.
17. Run Telegram sandbox E2E. The technical-debt register still correctly lists this as open. Exercise real commands, callbacks, linking, subscription, signals, feedback, paper controls, broker controls, referral flow, recovery-signal labels, and unknown-command handling against Telegram’s actual API.
18. Resolve or formally accept the remaining engineering debt before calling production “finished.” The repo still lists live-provider integration confidence, full Telegram E2E, incomplete decision-intelligence coverage across every lifecycle branch, the split outcome-tracker architecture, enterprise observability/SLOs, and the full premium UX/message rewrite as open items.
19. Add/finish production observability. Build dashboards for signal generation/delivery, provider freshness/failure, DB admission pressure, Telegram delivery, web fan-out, ML probability/starvation/drift, broker blocks/executions, outcome lag, payment/email failures, worker health, and service resource usage. Define alerts, SLOs, and incident runbooks.
20. Run load/failure drills. Simulate DB contention, Redis loss, provider timeout, Telegram outage, SMTP failure, Paystack replay, stale market data, broker rejection, worker restart, engine restart, analytics restart, and service/network failure. Confirm idempotency and recovery rather than just successful happy paths.
21. Verify backup/restore and rollback. Take a staging DB backup, restore it into an isolated environment, prove migrations and canonical identities survive, and confirm the application can be rolled back from c61ef… without corrupting 0040/0041 data.
22. Run at least a 24-hour clean staging soak on the final SHA. No fatal/task-crash patterns, schema errors, DB starvation, duplicate signals, duplicate deliveries, duplicate paper positions, provider freshness failures outside legitimate market closure, memory/CPU runaway, or unexplained worker restarts.
23. Collect meaningful paper/shadow performance evidence before risking real money. The new starvation/recovery logic should accumulate enough samples across crypto, FX, commodities, stocks, and indices. Compare champion vs challenger, pass rates, calibration, expected R, asset/regime performance, slippage assumptions, and rejection counterfactuals. Do not lower the certified threshold merely to increase signal volume.
24. Only then promote the exact staging-certified SHA to production. Pre-stage production expected SHA, back up production DB, migrate through 0040/0041, deploy roles in a controlled order, verify source/schema gates and canonical data, and keep rollback ready. Do not merge arbitrary newer commits during this cutover.
25. Keep automatic real-money execution disabled during initial production verification. Start with signals/paper and, if desired, owner-only manual execution. REAL_EXECUTION_ENABLED, AUTO_EXECUTION_ENABLED, AUTO_TRADE_ENABLED, and COPY_TRADE_ENABLED should remain off until broker/testnet/manual-canary evidence is clean.
26. Run a tightly restricted owner real-money canary only after that. Small account/risk, explicit platform/Telegram allowlist, restricted symbols, manual-confirmed execution first, hard daily drawdown, reconciliation, stale-quote and slippage gates, kill switch, and full broker evidence. Expand only from observed evidence—not a target win-rate assumption.
27. Finish legal/provider/commercial requirements before public live execution. Confirm market-data redistribution rights, broker/API terms, financial-promotion/signal/copy-trading obligations, privacy/data retention, public performance claims, subscription/refund terms, tax/business treatment, and any jurisdiction-specific requirements.
28. Update the release documents after staging is actually proven. Several completion documents still describe older 0038 staging evidence even though the repository head is now 0041. Once runtime certification is complete, replace historical wording with exact deployment IDs, final SHA, schema head, test counts, provider evidence, and soak evidence.
So the key point is: I would not call SignalRank finished yet. The bulk of the architecture and the large parity rebuild are implemented, but the latest implementation is not yet running on the real staging services. The immediate critical path is:
exact-head certification → 0041 migration → fast-forward staging → four-role boot → E2E/runtime validation → 24h soak → controlled production promotion → owner-only real-money canary.
I would keep adding improvements only where they strengthen those gates; I would not keep expanding features before this 42-commit gap is closed.