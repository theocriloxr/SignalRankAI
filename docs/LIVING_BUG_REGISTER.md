# Living Bug Register

Last updated: 2026-06-30
Owner: Engineering

Track every discovered bug with root cause, reproduction, severity, affected
files, resolution, verification tests, and regression prevention.

| ID | Title | Root Cause | Reproduction Steps | Severity | Affected Files | Resolution | Verification Tests | Regression Prevention | Status | Date Opened | Date Resolved |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BUG-C001 | Signal action keyboard emitted dead callback without signal id | Callback payload defaulted to `mt5_trade` when no `signal_id` existed. | Build `_build_signal_action_keyboard({"asset": "BTCUSDT"})` and inspect first-row callbacks. | Medium | `signalrank_telegram/commands.py` | Removed trade callback unless a signal id exists. | `tests/test_command_contracts.py` | Contract test asserts no dead callback. | Closed | 2026-06-29 | 2026-06-29 |
| BUG-C002 | Gemini audit command imported missing helper | `services.gemini_ml.audit_recent` was referenced but absent. | Run `/gemini_audit` path or import helper. | Medium | `services/gemini_ml.py`, `signalrank_telegram/commands.py` | Added DB-backed `audit_recent()` and command session usage. | `tests/test_command_contracts.py` | Command contract mocks session/helper. | Closed | 2026-06-29 | 2026-06-29 |
| BUG-C003 | TP parser ignored dict `tp` entries | Parser handled numeric/list values but not all dict target keys. | Call `_parse_tp_levels([{"tp": "102.5"}])`. | Low | `engine/realtime_outcome_tracker.py` | Added `price`, `tp`, `target`, and `value` dict support. | `tests/test_time_stop_outcome_persistence.py` | Parser contract test. | Closed | 2026-06-29 | 2026-06-29 |
| BUG-C004 | Engine Pulse reported zero after deploy despite DB activity | Pulse trusted imported GlobalStats even when counters were cold, and DB delivery fallback queried the wrong timestamp column. | Deploy, wait for pulse, observe `Total Scanned: 0` while signals/outcomes exist. | High | `engine/admin_pulse.py`, `engine/stats_manager.py`, `core/redis_global_stats.py` | Merged GlobalStats with DB evidence; fixed delivered timestamp fallback; enabled Redis-backed stats adapter with legacy property setters. | `tests/test_deploy_log_regressions.py` | Regression test simulates cold counters with DB evidence. | Closed | 2026-06-29 | 2026-06-29 |
| BUG-C005 | Duplicate risk-free notifications per user/asset | Risk-free idempotency was per signal id only, so multiple active same-market signals each notified the same user. | Multiple AAVEUSDT SHORT 1d active signals reach halfway to TP1. | Medium | `engine/realtime_outcome_tracker.py` | Added per-recipient asset/direction/timeframe cooldown. | `tests/test_deploy_log_regressions.py` | Recipient throttle test covers same and different timeframe behavior. | Closed | 2026-06-29 | 2026-06-29 |
| BUG-C006 | Optional Polygon outage alert was noisy | Optional fallback provider outage alerts were not suppressed by default. | Provider health marks `polygon_connector` down for 10 minutes. | Medium | `data/fetcher.py` | Added optional-provider outage suppression with opt-in env override. | `tests/test_deploy_log_regressions.py` | Optional outage suppression and opt-in tests. | Closed | 2026-06-29 | 2026-06-29 |
| BUG-C007 | TradingView analysis could emit synthetic zero-price candle | TradingView validator returned metadata shaped as OHLCV with zero prices. | Call `get_tradingview_candles()` when TradingView analysis succeeds. | High | `data/fetcher.py` | Removed placeholder candle; returns only real candle data from actual providers. | `tests/test_deploy_log_regressions.py` | Regression test ensures TradingView validation returns no synthetic candle. | Closed | 2026-06-29 | 2026-06-29 |
| BUG-C008 | Weekly admin report shadow counters could report zero | Weekly report referenced Redis `state` without importing it, then broad exception handling masked the failure. | Trigger weekly filter efficacy report while Redis shadow counters are populated. | Medium | `engine/admin_pulse.py` | Imported `state` in the shadow-counter block and added regression coverage for real counter values in the Telegram text. | `tests/test_deploy_log_regressions.py` | Weekly admin report test asserts shadow totals are included. | Closed | 2026-06-29 | 2026-06-29 |
| BUG-C009 | Similarity module failed project compile | `get_historical_winrate` used `async with` inside a synchronous function. | Run project-owned compile check or import `engine.similarity`. | High | `engine/similarity.py` | Converted `get_historical_winrate` to async and kept the sync wrapper path intact. | `tests/test_deploy_log_regressions.py`; project-owned compile check | Regression test imports module and exercises sync wrapper. | Closed | 2026-06-29 | 2026-06-29 |
| BUG-C010 | Partial TP signals could be re-sent as new exposure | Delivery dedupe treated `tp1`, `tp2`, breakeven, and partial states as resolved. | Receive same asset/direction after TP1/TP2 progress while trade is still open. | High | `db/pg_features.py` | Terminal-status set now excludes partial TP/progress states; unresolved block defaults to 168h. | `tests/test_delivery_limit_guard.py` | Regression asserts TP1/TP2 are not resolved for delivery dedupe. | Closed | 2026-06-30 | 2026-06-30 |
| BUG-C011 | Daily tier caps counted failed reservations | Delivery cap queries counted rows before Telegram send confirmation. | Telegram send fails or is dedup-updated; user quota still decreases. | High | `db/pg_features.py`, `core/tier_constants.py` | Cap counters now require `SignalDelivery.sent_ok IS TRUE`; tier defaults match free/premium/VIP/owner/admin product caps. | `tests/test_delivery_limit_guard.py` | Regression asserts confirmed-send counting and launch caps. | Closed | 2026-06-30 | 2026-06-30 |
| BUG-C012 | Updated signal jump button did not navigate | `Go to signal` was callback-only, so Telegram could not jump to the edited message. | Receive in-place signal update and click `Go to signal`. | Medium | `signalrank_telegram/bot.py` | Button now uses `_build_signal_message_link(...)` URL. | `tests/test_post_deploy_signal_hardening.py` | Regression asserts URL button and no callback payload. | Closed | 2026-06-30 | 2026-06-30 |
| BUG-C013 | TP notification could show `None` and `Ref: N/A` | Outcome formatter expected `symbol`/`id` while callers often had `asset`/`signal_id`. | TP notification for ORM-backed signal payload lacking formatter aliases. | Medium | `signalrank_telegram/bot.py`, `engine/tier_notifications.py` | Outcome payload normalized and formatter now falls back across symbol/ref/TP aliases. | `tests/test_post_deploy_signal_hardening.py` | Regression formats TP hit with only `asset`/`signal_id`. | Closed | 2026-06-30 | 2026-06-30 |
| BUG-C014 | Paystack webhook replay crashed on missing package import | `payments/paystack.py` imported `paystack.paystack.DURATIONS`, but the local `paystack` folder is not packaged in all runtime/test contexts. | Post Paystack charge.success webhook without `duration_days`. | High | `payments/paystack.py` | Added self-contained duration defaults and safe import fallback. | `tests/test_paystack_webhook.py`; full suite | Regression verifies idempotent replay does not crash. | Closed | 2026-06-30 | 2026-06-30 |
| BUG-C015 | Same asset could be delivered repeatedly inside 12 hours | Delivery gate matched same asset plus direction, and in-flight reservations could be retried before Telegram sent_ok was marked. | Receive repeated XAGUSD/BTCUSDT/USDCHF signals within minutes. | High | `db/pg_features.py`, `signalrank_telegram/bot.py`, `core/tier_constants.py` | Enforced same-user/same-asset 12h cooldown for all tiers/directions and added in-flight reservation guard. | `tests/test_delivery_limit_guard.py` | Regression asserts asset-wide lock and in-flight guard. | Closed | 2026-06-30 | 2026-06-30 |
| BUG-C016 | MT5 status could say linked while trade button was not executable | Credentials were saved locally, but no MetaApi account ID existed and the trade callback did not retry provisioning. | Link MT5, see linked status, then click trade button and receive no executable bridge message. | High | `services/mt5_client.py`, `signalrank_telegram/bot.py`, `signalrank_telegram/commands.py` | Added `ensure_user_mt5_account_id(...)` reprovision path and READY/NOT READY status messaging. | `tests/test_post_deploy_signal_hardening.py` | Regression asserts callback uses reprovision helper. | Closed | 2026-06-30 | 2026-06-30 |
| BUG-C017 | MarketMonitor crashed on timezone-aware alert timestamps | Alert rate limiter subtracted naive `datetime.utcnow()` from aware timestamps. | Run MarketMonitor after setting aware `last_no_trade_alert`. | Medium | `engine/signal_context.py`, `worker/market_monitor.py` | Normalize `now` to the previous timestamp's timezone before subtraction. | `tests/test_market_monitor_regressions.py` | Regression covers aware last-alert timestamp. | Closed | 2026-06-30 | 2026-06-30 |
| BUG-001 | Live Telegram workflow coverage gap | Not a code defect; E2E coverage is incomplete. | Run suite without Bot API sandbox and observe no full workflow send. | High | `signalrank_telegram/*` | Pending. | Planned sandbox E2E tests | Add gated E2E workflow suite. | Open | 2026-06-29 |  |
