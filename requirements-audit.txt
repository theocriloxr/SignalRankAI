# Install only in an isolated certification service, not the live Railway bot.
-r requirements.txt
coverage[toml]>=7.6
ruff>=0.8
mypy>=1.13
bandit>=1.7.10
pip-audit>=2.7
vulture>=2.13
hypothesis>=6.120
mutmut>=3.0
